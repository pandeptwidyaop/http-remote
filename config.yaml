server:
  host: "0.0.0.0"
  port: 8081
  path_prefix: "/devops"
  # allowed_origins:
  #   - "https://example.com"
  #   - "*.example.com"

database:
  path: "./data/deploy.db"

auth:
  session_duration: "24h"
  bcrypt_cost: 12

execution:
  default_timeout: 300
  max_timeout: 3600
  max_output_size: 10485760

admin:
  username: "admin"
  password: "changeme"

terminal:
  shell: "/bin/bash"
  args: ["-l"]
  env:
    - "SUDO_ASKPASS=/usr/bin/ssh-askpass"
  # enabled: true  # Set to false to disable terminal

# Security settings
# security:
#   # 32-byte encryption key for TOTP secrets (64 hex characters)
#   # Generate with: openssl rand -hex 32
#   encryption_key: "your-64-char-hex-key-here"
#
#   # Account lockout settings (brute force protection)
#   max_login_attempts: 5    # Lock after 5 failed attempts (default: 5)
#   lockout_duration: "15m"  # Lock for 15 minutes (default: 15m)
